# Developer Setup Guide

## Firebase Configuration

1. Copy `src/js/firebase-config.template.js` to `src/js/firebase-config.js`
2. Get your Firebase credentials from [Firebase Console](https://console.firebase.google.com/)
3. Replace the placeholder values in `firebase-config.js`
4. **Never commit** `firebase-config.js` to Git